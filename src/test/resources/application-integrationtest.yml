kafka:
  sedSendt:
    topic: eessi-basis-sedSendt-v1
    groupid: eessi-pensjon-group

spring:
  kafka:
    listener:
      ack-mode: manual
    consumer:
      enable-auto-commit: false
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    properties:
      security.protocol: PLAINTEXT

EUX_RINA_API_V1_URL: http://localhost:${mockServerport}
securitytokenservice.url: http://someurl
security-token-service-token.url: http://localhost:${mockServerport}
srvusername: someuser
srvpassword: somepass